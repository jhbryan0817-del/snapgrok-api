import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "SnapGrok — Capture the question. Keep your focus.",
  description: "SnapGrok is a Chrome MCQ capture assistant that delivers one-glance answers in your toolbar.",
  other: {
    "codex-preview": "development",
  },
  icons: {
    icon: "/snapgrok-icons/default.png",
    shortcut: "/snapgrok-icons/default.png",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
