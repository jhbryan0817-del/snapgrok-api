{
  "name": "snapgrok-api-secured",
  "version": "0.3.0",
  "private": true,
  "description": "Render-ready SnapGrok backend with Clerk authentication, strict CORS, rate limiting, and no persistent request-data storage.",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "check": "node --check src/server.js && node --check src/xai.js && node --check src/env.js"
  },
  "dependencies": {
    "@clerk/backend": "^3.11.7"
  },
  "engines": {
    "node": ">=20.9.0"
  }
}
